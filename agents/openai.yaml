interface:
  display_name: "Seedance Storyboard"
  short_description: "先确认制作参数，再产出剧本、素材表、分镜和续拍方案"
  icon_small: "./assets/seedance-small.svg"
  icon_large: "./assets/seedance.svg"
  brand_color: "#D9480F"
  default_prompt: "Use $seedance-storyboard-generator to confirm production parameters first, then produce the script, asset list, storyboard files, and episode chaining notes."

policy:
  allow_implicit_invocation: true
